// Copy this file to config.js (gitignored) and paste your Google Map Tiles API key.
// It's a CLIENT key — restrict it by HTTP referrer + API in Google Cloud Console.
// If config.js is absent, the app falls back to the keyless CARTO basemap.
window.GMAPS_KEY = "YOUR_MAP_TILES_API_KEY";
